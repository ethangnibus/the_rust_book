fn main() {
    // three types of loops:
    // loop, while, and for

    // use 'loop_name: loop { ... } then
    // break 'loop_name;
    // to break outer loops

    //Nth fibonacci number
    let highest_number = 100;
    let mut last = 0;

    for x in 1..=highest_number {
        println!("the current number is {x}")
    }
}
