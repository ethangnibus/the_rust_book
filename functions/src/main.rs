fn main() {
    let x: i32 = plus_one(5);

    println!("The value of x  is: {x}");
}

fn plus_one(x: i32) -> i32 {
    x + 1;

    // hello, world

    // So we're doing somehting complicated here, long enough
    // that we need multiple lines!
    // :)
}